server:
  port: 8084

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/neetinkoshti/user-rating-ms-repo
          clone-on-start: true
